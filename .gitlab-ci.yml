pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r docs/widoco-docs/* public/
    - cp public/index-en.html public/index.html
  artifacts:
    paths:
      - public
  only:
    - master
